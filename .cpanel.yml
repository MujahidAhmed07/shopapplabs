---
deployment:
  tasks:
    - export DEPLOYPATH=/home/aljadeedelectric/uchecker
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH && source /home/aljadeedelectric/nodevenv/uchecker/24/bin/activate && npm install --production=false && npm run build
    - touch $DEPLOYPATH/tmp/restart.txt
